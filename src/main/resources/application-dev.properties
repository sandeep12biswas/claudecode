# Active when running with -Dspring.profiles.active=dev (or SPRING_PROFILES_ACTIVE=dev).
# Verbose logging for local development, to check application logic as it runs.
logging.level.root=INFO
logging.level.org.example=DEBUG
