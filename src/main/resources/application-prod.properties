# Active when running with -Dspring.profiles.active=prod (or SPRING_PROFILES_ACTIVE=prod).
# Quiet logging in production: only error-level events from the application itself.
logging.level.root=WARN
logging.level.org.example=ERROR
